apiVersion: v1
kind: Service
metadata:
  name: {{ include "asv.fullname" . }}
  labels:
    {{- include "asv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "asv.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

